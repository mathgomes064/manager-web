import { ValuePipePipe } from './value-pipe.pipe';

describe('ValuePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ValuePipePipe();
    expect(pipe).toBeTruthy();
  });
});
